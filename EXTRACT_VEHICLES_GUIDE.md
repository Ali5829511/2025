# دليل استخدام نظام استخراج بيانات المركبات
# Vehicle Data Extraction System - User Guide

## نظرة عامة / Overview

هذا النظام يقوم بمعالجة مجلد يحتوي على صور للمركبات واستخراج البيانات التالية تلقائياً:
- **رقم اللوحة** (License Plate Number)
- **نوع المركبة** (Vehicle Type)
- **اللون السائد** (Dominant Color)
- **معلومات إضافية** (Additional metadata)

This system processes a folder of vehicle images and automatically extracts:
- **License plate numbers**
- **Vehicle types**
- **Dominant colors**
- **Additional metadata**

---

## المتطلبات الأساسية / Prerequisites

### 1. نظام التشغيل / Operating System
- ✅ Windows 10 أو أحدث
- ✅ Windows Server
- ✅ Linux / macOS (مدعوم أيضاً)

### 2. البرمجيات المطلوبة / Required Software

#### أ) Python 3.7 أو أحدث
**تحميل وتثبيت Python:**

1. انتقل إلى الموقع الرسمي: https://python.org
2. حمّل آخر إصدار من Python 3
3. **مهم جداً:** أثناء التثبيت، فعّل خيار **"Add Python to PATH"**
4. أكمل التثبيت بالضغط على "Install Now"

**التحقق من التثبيت:**
```bash
python --version
```
يجب أن ترى رقم الإصدار مثل: `Python 3.11.0`

#### ب) حساب Plate Recognizer
1. انتقل إلى: https://platerecognizer.com
2. سجل حساب جديد (مجاني للبداية)
3. احصل على رمز API الخاص بك من لوحة التحكم
4. احتفظ برمز API في مكان آمن

---

## الخطوة 1: تثبيت المكتبات المطلوبة

### طريقة 1: التثبيت التلقائي (موصى به)
افتح موجه الأوامر (Command Prompt) أو PowerShell وقم بتشغيل:

```bash
cd C:\Users\ALI\Desktop\2025
pip install -r requirements.txt
```

### طريقة 2: التثبيت اليدوي
إذا واجهت مشاكل مع الطريقة الأولى، قم بتثبيت كل مكتبة على حدة:

```bash
pip install requests
pip install pandas
pip install openpyxl
pip install pillow
pip install colorthief
```

### التحقق من التثبيت:
```bash
python -c "import requests, pandas, PIL, colorthief; print('✅ All libraries installed successfully')"
```

---

## الخطوة 2: تجهيز المجلد والصور

### 1. تنظيم الصور
- ضع جميع صور المركبات في مجلد واحد
- مثال: `E:\WhatsApp Chat - الامام المرور\6`
- الأنواع المدعومة: `.jpg`, `.jpeg`, `.png`, `.bmp`

### 2. متطلبات جودة الصور
- **الحد الأدنى للدقة:** 400 × 300 بكسل
- **الدقة الموصى بها:** 800 × 600 بكسل أو أعلى
- **وضوح اللوحة:** يجب أن تكون اللوحة واضحة وقابلة للقراءة
- **الإضاءة:** تجنب الصور شديدة الظلام أو شديدة السطوع

### 3. أمثلة على أسماء الصور الصحيحة:
```
IMG_1234.jpg
vehicle_001.jpeg
car-photo.png
20231115_143022.jpg
```

---

## الخطوة 3: تكوين السكربت

### 1. افتح ملف `extract_vehicles.py` في محرر نصوص

يمكنك استخدام:
- Notepad (المفكرة)
- Notepad++
- Visual Studio Code
- أي محرر نصوص آخر

### 2. عدّل الإعدادات التالية:

#### أ) مسار مجلد الصور:
```python
IMAGE_FOLDER = r"E:\WhatsApp Chat - الامام المرور\6"
```
**استبدل المسار بالمسار الخاص بك**

أمثلة:
```python
IMAGE_FOLDER = r"C:\Users\ALI\Pictures\Vehicles"
IMAGE_FOLDER = r"D:\Traffic\Photos"
IMAGE_FOLDER = r"E:\Work\Images"
```

#### ب) رمز API:
```python
API_TOKEN = "ضع رمز API الخاص بك هنا"
```

**مثال:**
```python
API_TOKEN = "sk_1234567890abcdef"
```

#### ج) اسم ملف النتائج (اختياري):
```python
OUTPUT_FILE = "نتائج_المركبات.xlsx"
```

يمكنك تغييره إلى:
```python
OUTPUT_FILE = "نتائج_المركبات_2024.xlsx"
OUTPUT_FILE = "vehicle_results.xlsx"
OUTPUT_FILE = "تقرير_اللوحات.xlsx"
```

### 3. إعدادات متقدمة (اختياري):

#### حجم الدفعة (لحفظ النتائج بشكل دوري):
```python
BATCH_SIZE = 50  # احفظ النتائج كل 50 صورة
```

#### التأخير بين الطلبات (لتجنب تجاوز الحد):
```python
API_DELAY = 0.5  # 0.5 ثانية بين كل طلب
```

#### الحد الأدنى للثقة:
```python
MIN_CONFIDENCE = 0.0  # قبول جميع النتائج
MIN_CONFIDENCE = 0.7  # قبول فقط النتائج بثقة 70% أو أعلى
```

#### المناطق (لتحسين الدقة):
```python
REGIONS = ['sa']  # السعودية
REGIONS = ['sa', 'ae']  # السعودية والإمارات
REGIONS = ['sa', 'ae', 'kw', 'qa']  # دول الخليج
```

---

## الخطوة 4: تشغيل السكربت

### طريقة 1: من موجه الأوامر

1. افتح موجه الأوامر (Command Prompt)
2. انتقل إلى المجلد الذي يحتوي على السكربت:
```bash
cd C:\Users\ALI\Desktop\2025
```

3. شغّل السكربت:
```bash
python extract_vehicles.py
```

### طريقة 2: من PowerShell

```powershell
cd C:\Users\ALI\Desktop\2025
python extract_vehicles.py
```

### طريقة 3: النقر المزدوج (إذا كان Python مرتبطاً بملفات .py)

ببساطة انقر نقراً مزدوجاً على ملف `extract_vehicles.py`

---

## الخطوة 5: فهم المخرجات

### أثناء التشغيل:

سترى رسائل مثل:
```
🚗 Vehicle Data Extraction System
🚗 نظام استخراج بيانات المركبات
============================================================

📋 Checking configuration...
✅ Configuration OK

📁 Scanning folder: E:\WhatsApp Chat - الامام المرور\6
📊 Found 8000 images to process

🚀 Starting processing...
------------------------------------------------------------

[1/8000] Processing: IMG_001.jpg
   🔍 Recognizing plate...
   🎨 Detecting color...
   ✅ Plate: ABC1234 (Confidence: 95.2%)

[2/8000] Processing: IMG_002.jpg
   🔍 Recognizing plate...
   🎨 Detecting color...
   ✅ Plate: XYZ5678 (Confidence: 89.7%)

...

💾 Saving intermediate results...
   ✅ Saved 50 results
```

### عند الانتهاء:

```
============================================================
📊 PROCESSING SUMMARY / ملخص المعالجة
============================================================
Total images:      8000 / إجمالي الصور
Successful:        7850 / نجح
Failed:            150 / فشل
Success rate:      98.1% / نسبة النجاح
Processing time:   4325.6 seconds / وقت المعالجة
Output file:       نتائج_المركبات.xlsx / ملف النتائج
============================================================

✅ Processing complete!
✅ اكتملت المعالجة!

📄 Results saved to: نتائج_المركبات.xlsx
```

---

## الخطوة 6: فهم ملف النتائج

### فتح ملف Excel:

1. انتقل إلى مجلد المشروع
2. افتح ملف `نتائج_المركبات.xlsx`
3. سترى جدول يحتوي على الأعمدة التالية:

| العمود | الوصف | مثال |
|--------|-------|------|
| **الرقم التسلسلي** | رقم تسلسلي للصورة | 1, 2, 3... |
| **اسم الملف** | اسم ملف الصورة | IMG_001.jpg |
| **رقم اللوحة** | رقم اللوحة المكتشف | ABC1234 |
| **الثقة** | مستوى الثقة (0-1) | 0.952 |
| **نوع المركبة** | نوع المركبة | Sedan, SUV, Truck |
| **اللون** | اللون السائد | أبيض - RGB(240, 240, 245) |
| **المنطقة** | كود المنطقة | sa, ae |
| **بدائل اللوحة** | أرقام لوحات بديلة | ABC1234, ABC1ZS4 |
| **الحالة** | حالة المعالجة | نجح، فشل |
| **رابط الصورة** | المسار الكامل للصورة | E:\...\IMG_001.jpg |
| **وقت المعالجة** | وقت ورخ المعالجة | 2024-11-19 18:30:45 |

---

## المميزات الخاصة

### 1. الاستئناف التلقائي (Resume Capability)

إذا توقف السكربت لأي سبب:
- يتم حفظ التقدم تلقائياً كل 50 صورة
- عند إعادة التشغيل، يستأنف من حيث توقف
- لن تتم معالجة الصور المكتملة مرة أخرى

### 2. الحفظ الدوري

- يتم حفظ النتائج كل 50 صورة (قابل للتعديل)
- إذا توقف البرنامج، لن تفقد البيانات
- ملف `نتائج_المركبات_progress.json` يحفظ التقدم

### 3. اكتشاف الألوان بالعربية

النظام يحول RGB إلى أسماء ألوان عربية:
- أبيض، أسود، رمادي
- أحمر، أزرق، أخضر
- أصفر، برتقالي، بنفسجي
- بني، بيج، كريمي

### 4. بدائل اللوحات

يوفر النظام حتى 3 قراءات بديلة لكل لوحة للتحقق اليدوي

---

## استكشاف الأخطاء وحلها

### خطأ: "API token not configured"
**الحل:**
- تأكد من تعديل `API_TOKEN` في السكربت
- تأكد من صحة رمز API الخاص بك

### خطأ: "Image folder not found"
**الحل:**
- تحقق من مسار المجلد في `IMAGE_FOLDER`
- تأكد من استخدام `r"المسار"` قبل المسار
- تأكد من وجود المجلد على جهازك

### خطأ: "No image files found"
**الحل:**
- تأكد من وجود صور في المجلد
- تحقق من أن الصور بالصيغ المدعومة (.jpg, .jpeg, .png, .bmp)

### خطأ: "Insufficient credits"
**الحل:**
- تحقق من رصيد حسابك في Plate Recognizer
- قد تحتاج إلى ترقية خطتك أو شراء رصيد إضافي

### خطأ: "Request timeout"
**الحل:**
- تحقق من اتصال الإنترنت
- قم بزيادة `API_DELAY` لإبطاء الطلبات

### خطأ: "Module not found"
**الحل:**
```bash
pip install --upgrade -r requirements.txt
```

---

## نصائح للحصول على أفضل النتائج

### 1. جودة الصور
- استخدم صور عالية الدقة (800x600 أو أعلى)
- تأكد من وضوح اللوحة في الصورة
- تجنب الصور المهتزة أو غير الواضحة

### 2. تحسين الدقة
- فعّل خيار `REGIONS` للمنطقة الجغرافية
- استخدم `MIN_CONFIDENCE` لتصفية النتائج الضعيفة
- تحقق من بدائل اللوحات للنتائج المشكوك فيها

### 3. الأداء
- عالج الصور في دفعات (50-100 صورة)
- استخدم `API_DELAY` المناسب لتجنب حظر API
- احفظ النتائج بشكل دوري

### 4. إدارة الموارد
- راقب استهلاك API من لوحة تحكم Plate Recognizer
- احذف ملف `_progress.json` بعد اكتمال المعالجة
- احتفظ بنسخة احتياطية من النتائج

---

## الأسئلة الشائعة (FAQ)

### س1: كم تكلفة استخدام Plate Recognizer API؟
**ج:** توفر Plate Recognizer خطة مجانية محدودة. للاستخدام المكثف (8000 صورة)، قد تحتاج إلى خطة مدفوعة. تحقق من أسعارهم على الموقع.

### س2: كم يستغرق معالجة 8000 صورة؟
**ج:** مع تأخير 0.5 ثانية بين كل طلب، حوالي 70-80 دقيقة. يمكنك تقليل التأخير إذا سمحت خطتك.

### س3: هل يمكن استخدام السكربت بدون اتصال إنترنت؟
**ج:** لا، يتطلب السكربت اتصال إنترنت للوصول إلى Plate Recognizer API.

### س4: هل يمكن معالجة صور من كاميرات المراقبة؟
**ج:** نعم، طالما الصور بجودة مقبولة واللوحات واضحة.

### س5: كيف أوقف السكربت وأستأنف لاحقاً؟
**ج:** اضغط Ctrl+C لإيقاف السكربت. سيتم حفظ التقدم تلقائياً. شغّل السكربت مرة أخرى للاستئناف.

### س6: هل يدعم النظام لوحات سعودية فقط؟
**ج:** لا، يدعم لوحات من جميع الدول. يمكنك تعديل `REGIONS` لتحسين الدقة للمناطق المختلفة.

### س7: كيف أحصل على دعم إضافي؟
**ج:** راجع:
- [Plate Recognizer Documentation](https://docs.platerecognizer.com/)
- [GitHub Issues](https://github.com/Ali5829511/2025/issues)

---

## المرحلة التالية - ميزات إضافية

بعد اكتمال المعالجة الأساسية، يمكنك:

### 1. تقسيم النتائج إلى دفعات
```python
# إضافة إلى السكربت لتقسيم النتائج
df_batch1 = df[0:1000]
df_batch2 = df[1000:2000]
# ... الخ
```

### 2. توليد رموز QR لكل صورة
```python
import qrcode
# كود لإنشاء QR codes للصور
```

### 3. إضافة حالة المعالجة
```python
# إضافة أعمدة إضافية:
# - تم المراجعة
# - الملاحظات
# - الإجراء المطلوب
```

### 4. ربط بـ Google Sheets
```python
# استخدام Google Sheets API للرفع التلقائي
```

### 5. إنشاء تقرير PDF
```python
# استخدام ReportLab لإنشاء تقرير مفصل
```

---

## الملاحظات المهمة

⚠️ **الخصوصية والأمان:**
- لا تشارك رمز API الخاص بك مع أحد
- احتفظ بالبيانات المستخرجة في مكان آمن
- راجع سياسة الخصوصية لـ Plate Recognizer

⚠️ **الاستخدام القانوني:**
- تأكد من امتلاك الحق في معالجة الصور
- راعِ قوانين الخصوصية والبيانات الشخصية
- استخدم البيانات للأغراض المصرح بها فقط

✅ **التحديثات:**
- تحقق من التحديثات على GitHub بشكل دوري
- قد تتوفر ميزات جديدة ومحسنة

---

## التواصل والدعم

للأسئلة والاستفسارات:
- 📧 البريد الإلكتروني: [من خلال GitHub]
- 🐛 الإبلاغ عن مشاكل: [GitHub Issues](https://github.com/Ali5829511/2025/issues)
- 📖 التوثيق: [Project Documentation](https://github.com/Ali5829511/2025)

---

## الترخيص

هذا النظام جزء من مشروع نظام إدارة إسكان أعضاء هيئة التدريس.

**استخدم هذا النظام بشكل مسؤول ووفقاً للقوانين المحلية.**

---

تم إعداد هذا الدليل بتاريخ: نوفمبر 2024
آخر تحديث: 2024-11-19
