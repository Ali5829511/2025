# 📖 دليل الاستخدام السريع - Quick Start Guide
## نظام إدارة المخالفات المرورية

---

## 🚀 البدء السريع في دقائق

### 1️⃣ التثبيت (5 دقائق)

```bash
# استنسخ المشروع
git clone https://github.com/Ali5829511/2025.git
cd 2025

# ثبّت المكتبات
pip install flask flask-cors python-dotenv requests pillow

# أنشئ قاعدة البيانات
python init_traffic_db.py

# شغّل التطبيق
python traffic_app.py
```

افتح المتصفح على: http://localhost:5001

---

## 🎯 الاستخدام الأساسي

### عرض المخالفات
1. افتح الصفحة الرئيسية `/`
2. ستشاهد قائمة بجميع المخالفات
3. استخدم البحث للتصفية

### إضافة مخالفة جديدة
1. انقر على "➕ إضافة مخالفة"
2. ارفع صورة المخالفة
3. سيتم اكتشاف رقم اللوحة تلقائياً (إذا كان API مفعّل)
4. أكمل البيانات المطلوبة
5. احفظ المخالفة

---

## 🔑 تفعيل Plate Recognizer (اختياري)

1. **سجل حساب مجاني:**
   - زر https://app.platerecognizer.com/
   - سجل حساب جديد
   - ستحصل على 2,500 استعلام مجاناً شهرياً

2. **احصل على API Token:**
   - من لوحة التحكم، انتقل إلى "API Keys"
   - انسخ المفتاح

3. **فعّل في التطبيق:**
   
   أنشئ ملف `.env`:
   ```
   PLATE_RECOGNIZER_API_TOKEN=your_token_here
   ```
   
   أو صدّر متغير البيئة:
   ```bash
   export PLATE_RECOGNIZER_API_TOKEN=your_token_here
   ```

4. **أعد تشغيل التطبيق**
   ```bash
   python traffic_app.py
   ```

---

## 🌐 النشر السريع

### خيار 1: Render.com (الأسهل)

1. سجل دخول على https://render.com
2. "New +" → "Web Service"
3. اختر المستودع
4. استخدم هذه الإعدادات:
   ```
   Build: pip install -r requirements.txt && python init_traffic_db.py
   Start: python traffic_app.py
   ```
5. أضف متغير البيئة `PLATE_RECOGNIZER_API_TOKEN`
6. انشر!

### خيار 2: Railway.app

1. سجل دخول على https://railway.app
2. "New Project" → "Deploy from GitHub"
3. اختر المستودع
4. أضف متغير البيئة `PLATE_RECOGNIZER_API_TOKEN`
5. انشر تلقائياً!

---

## 📱 استخدام الواجهة

### الصفحة الرئيسية
- **البحث:** اكتب في مربع البحث للتصفية
- **الصور:** انقر على أي صورة لعرضها بحجم كبير
- **الإحصائيات:** في الأعلى تشاهد:
  - إجمالي المخالفات
  - عدد السيارات
  - إجمالي الغرامات

### صفحة إضافة مخالفة
- **رفع صورة:** اسحب وأفلت أو انقر لاختيار
- **اكتشاف تلقائي:** سيظهر رقم اللوحة المكتشف
- **البيانات المطلوبة (*):** 
  - رقم اللوحة
  - نوع المخالفة
  - التاريخ
  - قيمة الغرامة

---

## 🔧 المشاكل الشائعة وحلولها

### 1. التطبيق لا يعمل
```bash
# تحقق من تثبيت المكتبات
pip install -r requirements.txt

# تحقق من قاعدة البيانات
python init_traffic_db.py
```

### 2. Plate Recognizer لا يعمل
- تأكد من صحة API Token
- تحقق من وجود رصيد متبقي
- اختبر الاتصال: افتح `/api/plate-recognizer/status`

### 3. الصور لا تظهر
```bash
# أنشئ المجلدات المطلوبة
mkdir -p static/uploads/violations
mkdir -p static/images
```

### 4. خطأ في قاعدة البيانات
```bash
# احذف وأعد إنشاء قاعدة البيانات
rm traffic.db
python init_traffic_db.py
```

---

## 📊 API Endpoints للمطورين

### الحصول على جميع المخالفات
```http
GET /api/violations
```

**Response:**
```json
{
  "success": true,
  "data": [...],
  "total": 4
}
```

### إضافة مخالفة جديدة
```http
POST /api/add-violation
Content-Type: application/json

{
  "plate_number": "ABC-1234",
  "violation_type": "تجاوز السرعة",
  "violation_date": "2024-01-15",
  "fine_amount": 500.0,
  "owner_name": "أحمد محمد",
  "model": "تويوتا كامري",
  "year": 2020,
  "color": "أبيض",
  "officer_name": "محمد أحمد",
  "image_path": "/static/uploads/violations/image.jpg"
}
```

### رفع صورة
```http
POST /api/upload-violation
Content-Type: multipart/form-data

image: [file]
```

### حالة Plate Recognizer
```http
GET /api/plate-recognizer/status
```

---

## 💡 نصائح وأفضل الممارسات

### للاستخدام الفعّال:
1. ✅ استخدم صور واضحة عالية الجودة
2. ✅ تأكد من ظهور اللوحة بوضوح في الصورة
3. ✅ استخدم البحث للوصول السريع للمخالفات
4. ✅ راجع الإحصائيات بانتظام

### للأمان:
1. 🔒 لا تشارك API Token
2. 🔒 استخدم HTTPS في الإنتاج
3. 🔒 احتفظ بنسخ احتياطية من قاعدة البيانات
4. 🔒 قيّد الوصول في بيئة الإنتاج

### للأداء:
1. ⚡ استخدم صور مضغوطة (< 5MB)
2. ⚡ نظف البيانات القديمة دورياً
3. ⚡ راقب استخدام API (2,500 شهرياً مجاناً)

---

## 📞 الحصول على المساعدة

### الموارد:
- 📖 README الكامل: [TRAFFIC_SYSTEM_README.md](TRAFFIC_SYSTEM_README.md)
- 🚀 دليل النشر: [TRAFFIC_DEPLOYMENT_GUIDE.md](TRAFFIC_DEPLOYMENT_GUIDE.md)
- 💬 GitHub Issues: [افتح issue جديد](https://github.com/Ali5829511/2025/issues)

### التوثيق الرسمي:
- Flask: https://flask.palletsprojects.com/
- Plate Recognizer: https://docs.platerecognizer.com/

---

## ⏱️ الجدول الزمني للاستخدام

| المهمة | الوقت المتوقع |
|--------|---------------|
| التثبيت والإعداد | 5 دقائق |
| إضافة أول مخالفة | 2 دقيقة |
| تفعيل Plate Recognizer | 5 دقائق |
| النشر على Render | 10 دقائق |
| **المجموع** | **~20 دقيقة** |

---

## ✅ قائمة التحقق السريعة

قبل البدء:
- [ ] تم تثبيت Python 3.8+
- [ ] تم تثبيت المكتبات المطلوبة
- [ ] تم إنشاء قاعدة البيانات
- [ ] التطبيق يعمل على localhost

للاستخدام المتقدم:
- [ ] تم الحصول على Plate Recognizer API Token
- [ ] تم تفعيل التعرف التلقائي
- [ ] تم اختبار رفع الصور
- [ ] تم النشر على السحابة

---

## 🎉 مبروك!

أنت الآن جاهز لاستخدام نظام إدارة المخالفات المرورية!

**ابدأ بإضافة أول مخالفة الآن 🚀**

---

**تطوير:** Ali5829511
**الترخيص:** MIT
**النسخة:** 1.0.0

🇸🇦 **صُنع بفخر في المملكة العربية السعودية**
