# دليل الاستخدام السريع - نظام تمييز اللوحات وكشف المخالفات
# Quick User Guide - Plate Recognition & Violation Detection System

## 🚀 الإعداد السريع / Quick Setup

### 1. إعداد رمز API في Render
1. اذهب إلى: https://dashboard.render.com/
2. اختر الخدمة: `two025-upa7`
3. اضغط على تبويب "Environment"
4. أضف المتغيرات التالية:
   ```
   PLATE_RECOGNIZER_API_TOKEN=22ba3cf7155a1ea730a0b64787f98ab5f9a3de94
   PARKPOW_CODE=7c13be422713a758a42a0bc453cf3331fbf4d346
   ```
5. احفظ وانتظر إعادة النشر (2-3 دقائق)

### 2. دمج التحديثات
1. اذهب إلى: https://github.com/Ali5829511/2025/pulls
2. اعثر على PR: "Fix pip root user warning in Docker build and login endpoint mismatch"
3. اضغط "Merge pull request"
4. أكد الدمج في branch الـ main

### 3. تسجيل الدخول
- الموقع: https://two025-upa7.onrender.com/
- اسم المستخدم: `admin`
- كلمة المرور: `Admin@2025`

## 📸 استخدام نظام تمييز اللوحات

### طريقة 1: تحليل صورة سيارة
1. سجل دخولك إلى النظام
2. اذهب إلى "تمييز لوحات السيارات" أو `plate_recognition.html`
3. اضغط على "اختر صورة" وارفع صورة للسيارة
4. اضغط "تمييز اللوحة"
5. سيظهر:
   - رقم اللوحة المكتشف
   - نسبة الدقة (Confidence)
   - معلومات السيارة إذا كانت مسجلة
   - سجل المخالفات (إن وجد)

### طريقة 2: استخدام كود برمجي (API)
```python
import plate_violation_detector

# تحليل صورة والتحقق من المخالفات
result = plate_violation_detector.process_vehicle_image(
    image_path='/path/to/car_image.jpg',
    user_id=1,
    location='مدخل السكن الشرقي',
    auto_detect_violations=True
)

if result['success']:
    print(f"رقم اللوحة: {result['plate_number']}")
    print(f"الدقة: {result['confidence']*100:.1f}%")
    
    if result['is_repeat_offender']:
        print("⚠️ مخالف متكرر!")
        print(f"عدد المخالفات: {result['total_violations']}")
        print("التوصيات:", result['recommendations_ar'])
```

## 📊 تصدير التقارير

### من الواجهة (plate_recognition.html):
1. اذهب إلى قسم "سجل التمييز"
2. (اختياري) حدد تاريخ البداية والنهاية
3. اختر نوع التصدير:
   - **Excel** - ملف إكسل شامل مع تنسيق
   - **PDF** - ملف PDF احترافي للطباعة
   - **HTML** - صفحة ويب قابلة للعرض والطباعة

### من الـ API:
```bash
# تصدير Excel
GET /api/plate-recognizer/export-excel?start_date=2024-01-01&end_date=2024-12-31

# تصدير PDF
GET /api/plate-recognizer/export-pdf?start_date=2024-01-01&end_date=2024-12-31

# تصدير HTML
GET /api/plate-recognizer/export-html?start_date=2024-01-01&end_date=2024-12-31
```

## 🚨 كشف المخالفات المتكررة

### الحصول على قائمة المخالفين المتكررين:
```python
import plate_violation_detector

# الحصول على المخالفين المتكررين (3+ مخالفات خلال 90 يوم)
offenders = plate_violation_detector.get_repeat_offenders(
    min_violations=3,
    days_period=90
)

for offender in offenders:
    print(f"لوحة: {offender['plate_number']}")
    print(f"المالك: {offender['owner_name']}")
    print(f"عدد المخالفات: {offender['violation_count']}")
    print(f"آخر مخالفة: {offender['last_violation']}")
    print(f"إجمالي الغرامات: {offender['total_fines']} ريال")
    print("---")
```

### تسجيل مخالفة تلقائياً:
```python
result = plate_violation_detector.auto_record_violation(
    vehicle_id=123,
    violation_type='توقف في مكان ممنوع',
    location='موقف المبنى رقم 5',
    description='سيارة متوقفة في موقف معاقين',
    fine_amount=500.00,
    user_id=1
)

if result['success']:
    print(f"تم تسجيل المخالفة برقم: {result['violation_id']}")
```

## 📈 فئات المخالفين

### 1. مخالف للمرة الأولى (First-time Offender)
- **العدد**: 0-2 مخالفات
- **الإجراء**: لا يوجد إجراء خاص

### 2. مخالف متكرر (Repeat Offender)
- **العدد**: 3+ مخالفات (كل الأوقات)
- **الإجراء**: إرسال إنذار

### 3. مخالف متكرر (Frequent Offender)
- **العدد**: 2+ مخالفات خلال 30 يوم
- **الإجراء**: إحالة للإدارة

### 4. مخالف خطير (Serious Offender)
- **العدد**: 5+ مخالفات خلال 90 يوم
- **الإجراء**: يُنصح بتثبيت المركبة

## 🔍 مراقبة الاستخدام

### التحقق من حالة API:
1. اذهب إلى: https://app.platerecognizer.com/service/snapshot-cloud/dashboard/
2. تسجيل الدخول بحسابك
3. تحقق من:
   - عدد الاستدعاءات المستخدمة: 16 / 50,000
   - الحد الأقصى: 8 طلبات/ثانية
   - تاريخ التجديد: 2025-12-01

### التحقق من عمل النظام:
```bash
# في الواجهة
1. سجل دخول
2. اذهب لتمييز اللوحات
3. ارفع صورة
4. إذا ظهرت النتيجة = النظام يعمل ✅
5. إذا ظهرت "خدمة Plate Recognizer غير مفعلة" = يحتاج تفعيل ⚠️
```

## ⚙️ الإعدادات المتقدمة

### تخصيص المناطق:
```python
# للكشف في منطقة محددة (مثلاً السعودية فقط)
result = plate_recognizer.recognize_plate_from_file(
    image_path='car.jpg',
    regions=['sa']  # Saudi Arabia
)
```

### دعم المناطق:
- ✅ السعودية (SA)
- ✅ الإمارات (UAE)
- ✅ مصر (EG)
- ✅ الأردن (JO)
- ✅ الكويت (KW)
- ✅ وأكثر من 100 دولة

## 🛠️ استكشاف الأخطاء

### المشكلة: "خدمة Plate Recognizer غير مفعلة"
**الحل:**
1. تأكد من إضافة `PLATE_RECOGNIZER_API_TOKEN` في Render
2. تأكد من صحة الرمز: `22ba3cf7155a1ea730a0b64787f98ab5f9a3de94`
3. أعد نشر الخدمة

### المشكلة: "حدث خطأ غير متوقع" عند تسجيل الدخول
**الحل:**
1. تأكد من دمج PR في الـ main branch
2. انتظر 2-3 دقائق بعد الدمج
3. امسح الـ cache وأعد تحميل الصفحة
4. استخدم: `admin` / `Admin@2025`

### المشكلة: التصدير لا يعمل
**الحل:**
1. تأكد من تسجيل الدخول
2. تحقق من وجود سجلات في قاعدة البيانات
3. تحقق من أذونات المستخدم
4. انظر في logs على Render

### المشكلة: الصورة لا تُحلّل
**الحل:**
1. تأكد أن الصورة واضحة وحجمها أقل من 5MB
2. استخدم صيغ مدعومة: JPEG, PNG
3. تأكد أن اللوحة ظاهرة بوضوح
4. جرّب من زاوية أمامية أو خلفية مباشرة

## 📞 الدعم

### للمشاكل التقنية:
- **GitHub Issues**: https://github.com/Ali5829511/2025/issues
- **Render Logs**: https://dashboard.render.com/ → Your Service → Logs

### لدعم Plate Recognizer API:
- **Email**: support@platerecognizer.com
- **Dashboard**: https://app.platerecognizer.com/
- **Docs**: https://docs.platerecognizer.com/

## ✅ قائمة التحقق النهائية

قبل الاستخدام تأكد من:
- [ ] دمج PR في main branch
- [ ] إضافة PLATE_RECOGNIZER_API_TOKEN في Render
- [ ] إضافة PARKPOW_CODE في Render (اختياري)
- [ ] إعادة نشر الخدمة على Render
- [ ] تسجيل الدخول ناجح
- [ ] تجربة رفع صورة وتمييز اللوحة
- [ ] تجربة تصدير Excel/PDF/HTML
- [ ] التحقق من سجل المخالفات

---

**آخر تحديث:** 9 نوفمبر 2025
**الإصدار:** 2.0 with Plate Recognizer Integration

🚀 النظام جاهز للاستخدام!
